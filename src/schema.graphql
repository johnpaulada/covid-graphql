type Query {
    countryStats: [CountryStats!]!
}

type CountryStats {
    country: String
    confirmed: Int
    recovered: Int
    deaths: Int,
    confirmedByDay: [Int!]!
    recoveredByDay: [Int!]!
    deathsByDay: [Int!]!
}